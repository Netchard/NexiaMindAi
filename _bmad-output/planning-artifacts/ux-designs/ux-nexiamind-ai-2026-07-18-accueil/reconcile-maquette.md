# Réconciliation maquette ↔ spine (accueil)

Notes pour la revue humaine — ce qui a été extrait avec confiance de `imports/maquette-nexiamind-ai-accueil-2026-07-18.html` (~9 Mo, grep ciblé `-oE`, jamais lu en entier) vs. ce qui a dû être assumé/hérité du thème sombre du chat.

- **Extrait avec confiance** : copie exacte du hero ("Toute la connaissance de l'entreprise, à portée de conversation." / sous-titre), du badge "Assistant RAG • en ligne", des 4 libellés de puces, du contenu complet des 3 étapes (titres + corps), du CTA de clôture "Commencer une conversation", et de toutes les valeurs CSS inline associées (couleurs hex/rgba, padding, border-radius, box-shadow, font-size/weight) — voir citations directes dans `DESIGN.md`.
- **Divergence tranchée (revue du 2026-07-18)** : le dégradé des CTA de la maquette (`#E2673C → #C14E2F`) était proche mais distinct du dégradé corail déjà établi dans le chat (`#F4693F → #E64F2B`). Décision produit : unifier — `hero-cta-gradient-from/to` reprennent désormais exactement les valeurs et l'ombre du chat. Aucune action dev au-delà de suivre `DESIGN.md` tel quel.
- **Police tranchée (revue du 2026-07-18)** : la maquette utilisait "Playfair Display" (non chargée dans `src/app/layout.tsx`, qui charge Geist/Geist Mono/Newsreader). Décision produit : réutiliser `Newsreader` (déjà chargée) plutôt qu'ajouter une seconde police serif — aucun nouveau `next/font/google` à câbler pour cette story.
- **Décision produit qui contredit la maquette** : le bouton "Explorer les documents" est rendu **actif/cliquable** dans la maquette (aucun style désactivé visible) ; `.memlog.md` exige qu'il soit désactivé avec tooltip "Bientôt disponible". Le traitement visuel de l'état désactivé (opacité 0.55, curseur interdit) est donc un `[ASSUMPTION]` inféré de la convention déjà utilisée ailleurs dans le produit (ex. `send-button` du chat), pas extrait de la maquette — à valider visuellement en implémentation.
- **Ordre des puces** : repris de l'ordre réel de la maquette (Codexia en premier, puis Résume/Explique/Où trouver) plutôt que de l'ordre de `.memlog.md` (qui liste Codexia en dernier) — `.memlog.md` documente le comportement de chaque puce, pas leur ordre visuel ; la maquette fait foi pour l'ordre.
- **Hors périmètre signalé, pas implémenté** : la maquette inclut une zone "avatar 3D" interactive (sphère Three.js, 560px, glisser pour tourner) à côté du hero, et son propre header/footer marketing de prototype. Aucun des deux n'est repris — le header/footer réel reste `Navbar`/`Footer` déjà stylés par le chat DESIGN.md, et l'avatar 3D n'a aucune story qui le couvre pour l'accueil.
- **Point d'architecture signalé, pas résolu** : le comportement "chercher une conversation Codexia" décrit dans `.memlog.md` s'appuie sur `useConversations()`, qui n'est aujourd'hui fourni que par `ConversationsProvider` scopé à `src/app/chat/layout.tsx` — pas disponible sur `/` en l'état. Deux options documentées dans `EXPERIENCE.md > Interaction Primitives` (remonter le provider au layout racine, ou appeler l'API sous-jacente directement) ; le choix est laissé à l'agent développeur/l'architecte.
