---
topic: Interface de chat conversationnel NexiaMind AI (ST-303)
updated: 2026-07-11T18:33
---

- (decision) Nouveau dossier UX dédié au chat (ux-nexiamind-ai-2026-07-04-chat) plutôt que d'étendre le dossier auth existant, pour ne pas écraser le DESIGN.md/EXPERIENCE.md finalisés de l'auth
- (decision) Sources confirmées : spine auth (DESIGN.md/EXPERIENCE.md), story dev ST-303 (4-303-creer-l-interface-de-chat.md), architecture.md (contrats API chat + schéma DB). PRD/brief/epics/story backend écartés, jugés déjà reflétés dans la story dev.
- (decision) Bulles : utilisateur aligné à droite, assistant à gauche (convention messagerie).
- (decision) Assistant a une identité visuelle propre : avatar/icône NexiaMind AI (logo losange bleu existant) à côté des messages assistant.
- (decision) État vide : message de bienvenue court + 2-3 questions suggérées cliquables pour amorcer l'usage (répond à NF-032).
- (decision) Historique des conversations : menu déroulant à la demande (pas de sidebar permanente), cohérent avec le périmètre ST-303 (bascule multi-conversation complète différée à ST-306).
- (decision) Zone de saisie : textarea auto-grandissante (multi-lignes, Shift+Entrée pour saut de ligne, Entrée seule soumet).
- (decision) Indicateur de chargement : points animés (typing indicator) dans la bulle assistant en attente, pas de spinner+texte.
- (decision) Espace citations/sources réservé (vide) sous la bulle assistant dès ST-303, pour que ST-305 s'y insère sans re-designer la bulle.
- (event) DESIGN.md et EXPERIENCE.md rédigés (draft) : bulles corail/neutre, avatar assistant, état vide avec suggestions, menu historique en overlay, textarea auto-grandissante, indicateur points animés, espace sources réservé, parcours Léa (suite du parcours auth).
- (event) Maquette key-screens générée (.working/key-chat.html) : état vide, conversation active, erreur d'envoi. Publiée en artefact pour revue.
- (decision) Couverture maquette confirmée : panneau historique ouvert construit depuis le spine seul (pattern déjà établi par UserMenu de la Navbar), pas de maquette dédiée.
- (event) Polish structurel et rédactionnel appliqués (aucun changement substantiel requis). DESIGN.md et EXPERIENCE.md passés en status: final.
- (decision) Pivot vers le thème sombre de docs/Maquette-ux-NexiaMind AI.html (import sauvegardé dans imports/maquette-nexiamind-ai-dark-2026-07-07.html). La maquette rend un app-shell complet (header 64px avec logo, nav Accueil/Chat/Recherche/Documents/Admin, sélecteur de source, bouton Rafraîchir, avatar utilisateur) qui correspond déjà 1:1 aux NAV_ITEMS et à la structure existante de src/components/Navbar/Navbar.tsx — seul l'habillage visuel change, pas l'IA. Le composant Footer n'apparaît pas dans la maquette : retiré de l'écran /chat pour respecter fidèlement la maquette (statut sur les autres pages non encore spécifiées laissé ouvert).
- (change) DESIGN.md et EXPERIENCE.md réécrits pour le thème sombre + tokens app-shell (header/nav/source-select/avatar) ; mockups/chat.html regénéré avec le header global inclus dans les 3 écrans clés.
- (event) spines finalized (dark theme pivot + app-shell tokens)
- (change) Ajout de la liste de conversations (sidebar permanente desktop >=lg, overlay plein-écran <lg) au périmètre du spine — supersède la décision ST-303 'menu déroulant, pas de sidebar permanente', la bascule multi-conversation ST-306 étant désormais implémentée.
- (decision) Titre de conversation trop long dans la sidebar : retour à la ligne libre, sans limite de lignes ni troncature (pas de line-clamp).
- (decision) Fond de la sidebar de conversations : réutilise {colors.surface-header} (même ton que la Navbar globale) plutôt que le ton du panneau de chat — l'ancre visuellement comme un élément structurel.
- (decision) Survol d'un item de conversation (non sélectionné) : fond corail translucide rgba(244,105,63,.08), même valeur que suggested-prompt-chip.hoverBackground déjà documenté — cohérent avec le seul autre hover corail existant sur cette surface.
- (change) Layout & Spacing : la zone de saisie doit rester visible en bas de l'écran sans nécessiter de scroll de page — le panneau /chat occupe exactement l'espace restant sous la Navbar globale (h-full, pas h-screen) plutôt que de forcer sa propre hauteur 100vh en plus de celle-ci.
- (event) Bugfix implémentation : 5 classes Tailwind chat-* référencées par les composants ST-306 (surface-sidebar, surface-hover, surface-input, border-input, primary-dark) n'existaient pas dans tailwind.config.ts — remplacées par les tokens existants équivalents (surface-header, surface-card, border, primary-active) et un nouveau token surface-hover ajouté.
- (event) Spines mis à jour (DESIGN.md + EXPERIENCE.md, status resté final) : périmètre étendu à ST-306 (sidebar de conversations), nouveaux tokens conversation-list-sidebar/conversation-item/conversation-actions-menu/surface-hover, contrainte de hauteur composer-toujours-visible documentée. mockups/chat.html non régénéré (ne couvre pas encore la sidebar) — laissé en spine-only pour cette mise à jour tactique.
- (event) Bugfix implémentation : la liste de messages avait un conteneur de scroll imbriqué et cassé (ChatMessageList portait son propre flex-1/overflow-y-auto, mais son parent direct dans page.tsx/[conversationId]/page.tsx n'était pas flex, rendant ce flex-1 inerte et empêchant le scroll de se limiter correctement entre le header et le composer). Fusionné en un seul conteneur de scroll (le wrapper parent, maintenant flex flex-col overflow-y-auto), ChatMessageList ne porte plus overflow-y-auto lui-même.
- (change) Reprise mise à jour (2026-07-11) : docs/Modification interfac.md documente une implémentation déjà réalisée hors process (AvatarPanel.tsx sous la liste de conversations, panneau de chat centré via layout.tsx, bouton Historique dans ConversationHeader) — reconciliation avec le spine plutôt que discovery from scratch, code pris comme source de vérité factuelle.
- (decision) AvatarPanel : les boutons 'Nouveau'/'Paramètres' initialement implémentés sont remplacés (pas ajoutés) par deux contrôles micro : (1) bouton bascule on/off (écoute continue), (2) bouton 'maintenir' lié au raccourci global F8 (appui = micro actif, relâchement = micro coupé), cliquable/maintenable à la souris en plus du raccourci clavier. Le doublon 'Nouvelle conversation' (lien '+ Nouvelle' en en-tête de sidebar existant + bouton 'Nouveau' de AvatarPanel) est résolu par cette suppression — le lien d'en-tête reste l'unique déclencheur dans la sidebar.
- (decision) Fonction du micro : dictée vers la zone de saisie (speech-to-text inséré dans ChatInput, l'utilisateur relit avant d'envoyer) — pas d'envoi vocal automatique. Nouvelle infrastructure requise : contexte React dédié (pattern ConversationsContext/AuthProvider) fourni au niveau ChatLayout pour relier AvatarPanel (déclencheur, dans la sidebar) et ChatInput (récepteur du texte, rendu par les pages) qui sont frères sans état de saisie partagé aujourd'hui.
- (assumption) Le bouton 'Paramètres' n'a pas de nouveau point d'entrée de remplacement (retiré sans substitut) — aucune story ne le scope actuellement ; à rouvrir si un besoin de paramètres surface.
- (event) Bug d'implémentation repéré pendant la reconciliation : AvatarPanel.tsx référence la classe Tailwind bg-chat-surface-field, absente de tailwind.config.ts (le token DESIGN.md 'surface-field' est mappé sur la clé Tailwind 'surface', voir usage déjà correct dans HistoryMenu.tsx bg-chat-surface) — à corriger en même temps que la réécriture du composant pour les boutons micro.
- (event) DESIGN.md et EXPERIENCE.md mis à jour (status resté final, updated: 2026-07-11) : ajout avatar-panel/avatar-panel-avatar/mic-toggle-button/mic-ptt-button (tokens + prose), Component Patterns (panneau Avatar, bascule micro, maintien F8, dictée→zone de saisie), State Patterns (écoute active, permission refusée, navigateur non supporté), primitive F8 globale, Accessibility Floor (aria-pressed, aria-live local, F8 en renfort jamais seul moyen).
- (event) Implémentation livrée : DictationContext (src/components/Dictation, Web Speech API fr-FR, raccourci global F8, garde mutuelle bascule/maintien) fourni au niveau ChatLayout ; AvatarPanel réécrit (2 boutons micro remplacent Nouveau/Paramètres, zone avatar 3D inchangée) ; ChatInput consomme le transcript et l'insère dans la zone de saisie sans envoi automatique. Bug bg-chat-surface-field corrigé au passage (classe Tailwind inexistante, remplacée par bg-chat-surface comme HistoryMenu). Tests : 2 tests existants adaptés (DictationProvider requis), 4 nouveaux tests d'intégration ajoutés (bascule, F8 maintien, exclusion mutuelle, navigateur non supporté) — 23/23 verts. Vérifié : compile propre sur le serveur dev déjà lancé par l'utilisateur (port 3000), pas d'erreur runtime nouvelle. Échecs pré-existants et non liés (ChatMessage.test.tsx, page.test.tsx, page-filters.test.tsx — gaps de setup Markdown/provider antérieurs à cette session) laissés en l'état, hors périmètre.
- (change) Décision inversée (demande utilisateur, 2026-07-11) : la bulle assistant redevient sombre (colors.assistant-bubble-bg #0C1829, texte #EEF2F8) au lieu du fond clair précédemment documenté comme 'rupture de contraste volontaire, à préserver'. Cohérent avec Markdown.module.css qui était déjà écrit en texte clair sur fond sombre (le rendu Markdown enrichi ST-307 n'a jamais vraiment respecté l'ancien token clair — bug de contraste latent corrigé au passage).
- (decision) Ajout du bouton Écouter (lecture à voix haute, synthèse vocale Web Speech API fr-FR) dans la barre d'actions de chaque bulle assistant, à côté du bouton Export existant (ST-308, conservé). Une seule lecture active à la fois tous messages confondus (nouveau cancel() coupe l'ancienne).
- (event) Bugfix implémentation (double scroll imbriqué, violait la règle DESIGN.md déjà documentée 'un seul conteneur de scroll par zone') : (1) sidebar — le wrapper dans layout.tsx portait overflow-y-auto EN PLUS du flex-1 overflow-y-auto interne de ConversationList, et le conteneur racine de ConversationList passait en lg:block (non-flex) au lieu de lg:flex, rendant son flex-1 interne inerte — AvatarPanel scrollait avec la liste au lieu de rester fixe en bas. Corrigé : wrapper en overflow-hidden, ConversationList en lg:flex + h-full. (2) zone de messages — ChatMessageList portait son propre overflow-y-auto en plus de celui du wrapper de page (page.tsx/[conversationId]/page.tsx) — retiré de ChatMessageList, le wrapper de page reste l'unique conteneur de scroll (pattern déjà établi lors d'un bugfix similaire du 2026-07-09).
- (event) Vérification : suite de tests chat (Chat/ConversationList/Dictation/app/chat) exécutée après les fixes scroll + couleur + bouton Écouter — mes ajouts/modifs (ChatInput, ConversationList, Dictation, ChatMessage, ListenButton) sont tous verts (27 tests). Échecs restants (ConversationHeader.test.tsx, ChatMessage.test.tsx markdown, page.test.tsx, page-filters.test.tsx) confirmés préexistants (aucun diff de ma part sur ces fichiers de test, ni sur ConversationHeader.tsx dont le diff date d'avant cette session) — hors périmètre. ESLint et le serveur dev déjà actif (port 3000) restent propres.
- (change) Ossature révisée (2026-07-11) selon docs/Maquette-structure-NexiaMind-AI-Chat.html (fichier bundlé, décodé via ses repères data-screen-label : header/listeconversation/avatar/headechange/echangecenter/footechange/footer). Deux inversions de décisions antérieures, arbitrées par l'utilisateur : (1) le panneau de chat n'est plus une carte flottante centrée (bordure/ombre/radius/max-width 880px retirés) — pleine largeur de la colonne, comme avant le pivot du 2026-07-09 sur ce point précis. (2) Un footer global (48px, sombre, app-footer) réapparaît en bas de toute la surface /chat — supersède la décision du 2026-07-09 qui l'avait explicitement retiré. Contenu retenu : uniquement le disclaimer légal (pas le Footer marketing complet), déplacé depuis le pied du panneau de chat vers cette bande unique commune à toute la page (une instance au lieu de 4 dupliquées par état/page).
- (event) Implémentation : layout.tsx restructuré en flex-col (rangée sidebar+panneau flex-1, footer flex-none 48px) ; chat-panel perd mx-auto/max-w-[880px]/border/rounded/shadow ; disclaimer légal retiré de 4 endroits (page.tsx x3, [conversationId]/page.tsx x1) et centralisé dans le nouveau <footer> de layout.tsx. DESIGN.md/EXPERIENCE.md mis à jour (spacing.chat-max-width remplacé par spacing.footer-height, nouveau token app-footer, Components/Do's-Don'ts/Layout & Spacing/Elevation/Shapes ajustés).
- (event) Vérification post-restructuration : suite de tests chat repassée, même bilan qu'avant (79 verts / 37 échecs préexistants et confirmés sans rapport). ESLint propre (aucune nouvelle erreur/warning). Serveur dev déjà actif (port 3000) recompile sans erreur.
- (event) Bugfix racine (2026-07-11, retour utilisateur 'toujours pas ça' — la page entière défilait, pas juste chat-message-list) : le vrai coupable était src/app/layout.tsx, body en min-h-screen (plancher sans plafond) au lieu de h-full — aucune hauteur bornée ne descendait jusqu'à MainContent (main flex-1 overflow-hidden pour /chat), donc tout le chain overflow-hidden déjà en place (chat/layout.tsx, chat-panel) restait inerte et le body/viewport défilait nativement, entraînant le composer hors écran. Corrigé : body → h-full (borné comme html), sans overflow-hidden (les autres routes gardent leur scroll de page normal, seul /chat clippe explicitement via MainContent). Complété par : ChatMessageList redevient l'unique conteneur overflow-y-auto (data-testid=chat-message-list) ; les wrappers intermédiaires (page.tsx, [conversationId]/page.tsx, MainContent) passent en overflow-hidden/min-h-0 plutôt que overflow-y-auto, pour qu'un seul niveau ne scrolle par zone. Vérifié : /, /auth/login, /recherche, /documents répondent toujours normalement (pas de régression du scroll de page sur les autres routes) ; suite de tests chat inchangée (79 verts, mêmes 37 échecs préexistants).
- (event) Bugfix data-flow (retour utilisateur : 'la question ne s'affiche pas, il faut recharger la page') — deux causes réelles trouvées dans src/app/chat/layout.tsx (ConversationsProvider), aucune liée aux changements précédents de cette session : (1) onSendMessage n'ajoutait JAMAIS le message utilisateur à l'état local, seule la réponse assistant était appendée après l'appel API — le message existait bien côté serveur (d'où sa réapparition après reload/refetch complet) mais jamais affiché en direct. Corrigé : message utilisateur optimiste ajouté avant l'appel réseau (role='user', createdAt local), marqué failed:true en cas d'erreur au lieu de disparaître (aligné sur EXPERIENCE.md > State Patterns déjà documenté mais jamais implémenté). (2) src/app/chat/page.tsx : handleSend (état vide/chargement, donc TOUTE première question d'une session) appelait onCreateNewConversation() puis naviguait sans jamais transmettre le paramètre content — le texte tapé était perdu en silence (confirmé par le warning ESLint 'content is defined but never used' repéré plus tôt sans être investigué). Corrigé : délègue à onSendMessage(null, content, {}), qui gère déjà création+navigation+envoi. (3) Bug connexe repéré en corrigeant (1) : la réponse assistant stockait ses sources sous une clé 'citations' absente du type ConversationMessage (sources) au lieu de 'sources' — les citations de la dernière réponse n'apparaissaient donc pas non plus avant reload. Corrigé (sources: response.sources). ConversationMessage a gagné failed?:boolean, propagé par currentMessages vers ChatMessageData (déjà consommé par ChatMessageList pour l'indicateur "Non envoyé").
